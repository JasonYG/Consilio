class Material {
  constructor(initX, initY, initZ) {
    this.x = initX;
    this.y = initY;
    this.x = initZ;
  }


}

class formatDate extends Date {
  constructor(dateStr) {
    super(dateStr);
  }
}
